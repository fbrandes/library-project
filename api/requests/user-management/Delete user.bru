meta {
  name: Delete user
  type: http
  seq: 23
}

delete {
  url: {{userManagementBaseUrl}}/users/:id
  body: none
  auth: none
}

params:path {
  id: {{userManagementUserId}}
}

headers {
  Accept: application/json
}
